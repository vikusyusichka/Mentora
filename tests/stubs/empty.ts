/**
 * Заглушка для пакета `server-only` у тестах.
 *
 * Сам пакет навмисно кидає помилку при звичайному імпорті — так він і
 * захищає серверний код від потрапляння в клієнтський бандл. Але в тестах
 * ми виконуємо саме серверний код у Node, тож підміняємо його порожнім
 * модулем (у Next ту саму роль грає умова `react-server`).
 */
export {};
